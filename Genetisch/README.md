# Optimierung mit Genetischen Algorithmen -- PIKAIA

## Build
Build the code using the provided makefile by typing `make`.

## Run
Run the program using the `run.sh` script.
It fits the data and uses gnuplot to visualize the fits.
